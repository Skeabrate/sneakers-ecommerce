import React from 'react';
import PropTypes from 'prop-types';

const Wishlist = props => {
   return (
      <div>
         Wishlist
      </div>
   );
};

Wishlist.propTypes = {
   
};

export default Wishlist;